import { AuthConfig } from '../shared/spotify-auth-config.i';
import { Observable } from 'rxjs/Observable';
export declare class AuthService {
    private requestAuthUrl;
    private authorized$;
    private authConfig;
    authorize(): void;
    authorized(): void;
    readonly authorizedStream: Observable<boolean>;
    configure(config: AuthConfig): AuthService;
    private buildAuthUrl();
}
