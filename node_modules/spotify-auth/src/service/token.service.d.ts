import { Observable } from "rxjs/Observable";
import { SpotifyAuthResponse } from "../shared/spotify-auth-response.i";
export declare class TokenService {
    private token;
    private token$;
    readonly oAuthToken: string;
    clearToken(): void;
    readonly authHeader: {
        [name: string]: string;
    };
    readonly authTokens: Observable<string>;
    setAuthToken(spotifyResponse: SpotifyAuthResponse): boolean;
}
