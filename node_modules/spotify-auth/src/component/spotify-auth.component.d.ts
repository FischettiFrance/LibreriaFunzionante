import { OnInit } from '@angular/core';
import { AuthService } from '../service/auth.service';
export declare class SpotifyAuthComponent implements OnInit {
    private authService;
    constructor(authService: AuthService);
    ngOnInit(): void;
}
